import 'package:flutter/material.dart';

class TheEndMobilePortrait extends StatelessWidget {
  const TheEndMobilePortrait({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
