// It wraps the http package so we don't have to directly call methods 
// from the package. This will be really helpful in case we decide to change a
// package, we won't have to start editing our code at different places